// Replace this before publishing. Orbit uses it for every GitHub/contact link.
const ORBIT_REPOSITORY = 'https://github.com/AdjectiveYokai/orbit-launcher';
document.querySelectorAll('[data-repository]').forEach(link => { link.href = ORBIT_REPOSITORY; });

